# Copying this file is optional; make init-env generates a random password.
NEO4J_PASSWORD=replace-with-a-long-local-password
IDEA_DB_PORT=8080
# Optional bearer token for API access; keep it client-side.
# IDEA_DB_TOKEN=

